import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("sosats")
                .font(.title)
            Text("pirmais liga")
        }
        .padding()
    }
}
