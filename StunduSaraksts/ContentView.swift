import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Mans stundu saraksts")
                .font(.title)
            Text("Pirmais build strādā! 🎉")
        }
        .padding()
    }
}
